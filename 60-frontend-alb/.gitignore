# (Optional) Ignore lock file
.terraform.lock.hcl

# Terraform state files
*.tfstate
*.tfstate.*
**/.terraform/*
*.tfstate
*.tfstate.*
.terraform.lock.hcl
# tf vars contains some secure or sensitive data so we are not commiting the code
*.tfvars
*.tfvars.json